using System.Data.SqlTypes;

namespace PalindromeExercise;

public struct WordSmith
{
    public  bool IsPalindrome(string word)
    {
        if (string.IsNullOrEmpty(word))
            return false;
        var wordLength = new WordSmith();
        return true;
    } 
    
}   